function mostrar()
{

	var contador;
	var respuesta;
	var elMaximo;
	var elMinimo;

	contador = 0;

	numero = prompt ("Ingrese un número");
	numero = parseInt (numero);
	respuesta = prompt ("Queres seguir pidiendo números (si o no)");

	while (respuesta == "si") 
	{
		contador = contador + 1;
		numero = prompt ("ingrese otro número");
		numero = parseInt (numero);
		respuesta = prompt ("Queres seguir pidiendo números (si o no)");
	
	}	
	maximo.value = elMaximo;
	minimo.value = elMinimo



while ( isNan (nota) == true || sexo


}//FIN DE LA FUNCIÓN